.ink-hero-video-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
  opacity: 0.90;
  overflow: hidden;
}
#yt-player-target {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}
#yt-player-target iframe,
.ink-hero-video-bg iframe {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100vw !important;
  height: 56.25vw !important;
  min-height: 100vh !important;
  min-width: 177.77vh !important;
  transform: translate(-50%, -50%) scale(1.2);
  border: none !important;
  pointer-events: none !important;
  filter: brightness(0.85) contrast(1.08) saturate(1.2);
}

/* Center-Focused Dark Scrim Overlay (z-index: 10) - Dark in middle (0.58), light at margins (0.12) */
.ink-hero-video-scrim {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(ellipse 70% 55% at 50% 50%, rgba(10, 4, 18, 0.58) 0%, rgba(8, 2, 14, 0.35) 60%, rgba(6, 1, 10, 0.12) 100%);
  z-index: 10;
  pointer-events: none;
}

/* Radiant Backlight Vignette (z-index: 15) */
.hero-glow-backlight {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: clamp(350px, 60vw, 850px);
  height: clamp(350px, 60vw, 850px);
  background: radial-gradient(circle, rgba(123, 47, 255, 0.28) 0%, rgba(77, 16, 130, 0.14) 50%, transparent 75%);
  pointer-events: none; z-index: 15;
  filter: blur(60px);
}

/* Floating 3D WebGL Canvas Layer (z-index: 20) with Rim Glow & Drop Shadow */
.floating-elements-container {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 20;
  pointer-events: none;
  overflow: hidden;
}
#three-canvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100% !important;
  height: 100% !important;
  pointer-events: none;
  filter: drop-shadow(0 14px 28px rgba(0, 0, 0, 0.85)) drop-shadow(0 0 20px rgba(168, 85, 247, 0.35));
}