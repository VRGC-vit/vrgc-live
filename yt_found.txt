{"step_index": 94, "source": "MODEL", "type": "PLANNER_RESPONSE", "status": "DONE", "created_at": "2026-09-03T05:35:34Z", "thinking": "**Constructing Full Background Player**\n\nNeed to construct the complete `script.js`.  This involves defining a video array and implementing a shuffle function. The startup should trigger the shuffle. An initialization strategy is required: use the YouTube IFrame API if available; otherwise, default to a direct iframe implementation.\n\n\n", "tool_calls": [{"name": "write_to_file", "args": {"CodeContent": "/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   VRGC VIT BHOPAL \u2014 SHARED SCRIPT & CINEMATIC VIDEO ENGINE\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\n'use strict';\n\n/* \u2500\u2500 PRELOADER \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const loader = document.getElementById('preloader');\n  const bar    = document.getElementById('pre-bar');\n  const pct    = document.getElementById('pre-pct');\n  if (!loader) return;\n  let progress = 0;\n\n  const interval = setInterval(() => {\n    const step = Math.random() * 12 + 4;\n    progress = Math.min(progress + step, 95);\n    if (bar) bar.style.width = progress + '%';\n    if (pct) pct.textContent = Math.round(progress) + '%';\n  }, 80);\n\n  window.addEventListener('load', () => {\n    clearInterval(interval);\n    if (bar) bar.style.width = '100%';\n    if (pct) pct.textContent = '100%';\n    setTimeout(() => {\n      loader.classList.add('done');\n      triggerHeroAnimations();\n    }, 380);\n  });\n\n  // Fallback\n  setTimeout(() => {\n    if (progress < 95) {\n      clearInterval(interval);\n      if (bar) bar.style.width = '100%';\n      if (pct) pct.textContent = '100%';\n      setTimeout(() => { loader.classList.add('done'); triggerHeroAnimations(); }, 250);\n    }\n  }, 2500);\n})();\n\n/* \u2500\u2500 CUSTOM CURSOR \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const dot  = document.getElementById('cursor-dot');\n  const ring = document.getElementById('cursor-ring');\n  if (!dot || !ring) return;\n\n  let mx = 0, my = 0, rx = 0, ry = 0;\n\n  document.addEventListener('mousemove', e => {\n    mx = e.clientX; my = e.clientY;\n    dot.style.left = mx + 'px'; dot.style.top = my + 'px';\n  });\n\n  (function lerp() {\n    rx += (mx - rx) * 0.15; ry += (my - ry) * 0.15;\n    ring.style.left = rx + 'px'; ring.style.top = ry + 'px';\n    requestAnimationFrame(lerp);\n  })();\n\n  const sel = 'a, button, .game-card, .ev-card, .ev-sm-card, .ev-featured-card, .feat-card, .coord-circle, .dept-card, .soc-card, .sw-card, .spotlight-card, .top-card, .lab-photo, .hud-btn, .hud-dot';\n\n  document.addEventListener('mouseover', e => { if (e.target.closest(sel)) ring.classList.add('hover'); });\n  document.addEventListener('mouseout',  e => { if (e.target.closest(sel)) ring.classList.remove('hover'); });\n  document.addEventListener('mousedown', () => ring.classList.add('click'));\n  document.addEventListener('mouseup',   () => ring.classList.remove('click'));\n  document.addEventListener('mouseleave', () => { dot.style.opacity='0'; ring.style.opacity='0'; });\n  document.addEventListener('mouseenter', () => { dot.style.opacity='1'; ring.style.opacity='1'; });\n})();\n\n/* \u2500\u2500 NAVBAR SCROLL \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const nav = document.getElementById('navbar');\n  if (!nav) return;\n\n  const onScroll = () => {\n    nav.classList.toggle('scrolled', window.scrollY > 40);\n  };\n  window.addEventListener('scroll', onScroll, { passive: true });\n  onScroll();\n\n  const sections = document.querySelectorAll('section[id]');\n  const navLinks = document.querySelectorAll('.nl');\n  if (sections.length && navLinks.length) {\n    const obs = new IntersectionObserver(entries => {\n      entries.forEach(entry => {\n        if (entry.isIntersecting) {\n          navLinks.forEach(l => l.classList.remove('active'));\n          const match = document.querySelector(`.nl[href=\"#${entry.target.id}\"]`);\n          if (match) match.classList.add('active');\n        }\n      });\n    }, { rootMargin: '-40% 0px -50% 0px' });\n    sections.forEach(s => obs.observe(s));\n  }\n})();\n\n/* \u2500\u2500 MOBILE MENU \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const btn  = document.getElementById('hamburger');\n  const menu = document.getElementById('mob-menu');\n  if (!btn || !menu) return;\n  let open = false;\n\n  const toggle = () => {\n    open = !open;\n    btn.classList.toggle('open', open);\n    menu.classList.toggle('open', open);\n    document.body.classList.toggle('menu-open', open);\n    btn.setAttribute('aria-expanded', String(open));\n  };\n\n  btn.addEventListener('click', toggle);\n  menu.querySelectorAll('a').forEach(a => a.addEventListener('click', () => { if (open) toggle(); }));\n})();\n\n/* \u2500\u2500 SCROLL REVEAL \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const items = document.querySelectorAll('.ri');\n  const obs = new IntersectionObserver(entries => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        entry.target.classList.add('visible');\n        obs.unobserve(entry.target);\n      }\n    });\n  }, { rootMargin: '0px 0px -40px 0px', threshold: 0.06 });\n  items.forEach(el => obs.observe(el));\n})();\n\n/* \u2500\u2500 HERO TRIGGER \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nfunction triggerHeroAnimations() {\n  const vp = window.innerHeight;\n  document.querySelectorAll('.ri').forEach(el => {\n    const r = el.getBoundingClientRect();\n    if (r.top < vp * 0.95) el.classList.add('visible');\n  });\n}\n\n/* \u2500\u2500 COUNTERS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const els = document.querySelectorAll('.hs-num[data-count]');\n  const obs = new IntersectionObserver(entries => {\n    entries.forEach(entry => {\n      if (!entry.isIntersecting) return;\n      const el  = entry.target;\n      const end = parseInt(el.dataset.count, 10);\n      const dur = 1600;\n      const t0  = performance.now();\n      (function tick(now) {\n        const prog = Math.min((now - t0) / dur, 1);\n        const ease = 1 - Math.pow(1 - prog, 3);\n        el.textContent = Math.round(ease * end).toLocaleString();\n        if (prog < 1) requestAnimationFrame(tick);\n      })(t0);\n      obs.unobserve(el);\n    });\n  }, { threshold: 0.4 });\n  els.forEach(el => obs.observe(el));\n})();\n\n/* \u2500\u2500 COUNTDOWN TIMER \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const target = new Date('2025-10-18T09:00:00').getTime();\n  const pad    = n => String(n).padStart(2, '0');\n\n  const update = () => {\n    const diff = Math.max(0, target - Date.now());\n    const d = Math.floor(diff / 86400000);\n    const h = Math.floor((diff % 86400000) / 3600000);\n    const m = Math.floor((diff % 3600000) / 60000);\n    const s = Math.floor((diff % 60000) / 1000);\n\n    const setEl = (id, val) => { const el = document.getElementById(id); if (el) el.textContent = pad(val); };\n    setEl('ev1d', d); setEl('ev1h', h); setEl('ev1m', m); setEl('ev1s', s);\n    setEl('cd-d', d); setEl('cd-h', h); setEl('cd-m', m); setEl('cd-s', s);\n  };\n\n  update();\n  setInterval(update, 1000);\n})();\n\n/* \u2500\u2500 DRAG SCROLL (GAMES & LAB STRIP) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  ['games-outer', 'lab-strip-outer'].forEach(id => {\n    const el = document.getElementById(id);\n    if (!el) return;\n\n    let isDragging = false, startX, scrollLeft;\n\n    el.addEventListener('mousedown', e => {\n      isDragging = true; startX = e.pageX - el.offsetLeft; scrollLeft = el.scrollLeft;\n      el.style.cursor = 'grabbing'; e.preventDefault();\n    });\n    document.addEventListener('mouseup', () => { isDragging = false; el.style.cursor = 'grab'; });\n    document.addEventListener('mousemove', e => {\n      if (!isDragging) return;\n      const x = e.pageX - el.offsetLeft;\n      el.scrollLeft = scrollLeft - (x - startX) * 1.4;\n    });\n\n    let tx, ts;\n    el.addEventListener('touchstart', e => { tx = e.touches[0].pageX; ts = el.scrollLeft; }, { passive: true });\n    el.addEventListener('touchmove',  e => { el.scrollLeft = ts + (tx - e.touches[0].pageX); }, { passive: true });\n  });\n})();\n\n/* \u2500\u2500 SMOOTH SCROLL \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  document.querySelectorAll('a[href^=\"#\"]').forEach(a => {\n    a.addEventListener('click', e => {\n      const id = a.getAttribute('href').slice(1);\n      const target = document.getElementById(id);\n      if (!target) return;\n      e.preventDefault();\n      const top = target.getBoundingClientRect().top + window.scrollY - 72;\n      window.scrollTo({ top, behavior: 'smooth' });\n    });\n  });\n})();\n\n/* \u2500\u2500 JOIN FORM \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const form = document.getElementById('join-form');\n  const fb   = document.getElementById('form-fb');\n  const btn  = document.getElementById('join-submit');\n  if (!form) return;\n\n  form.addEventListener('submit', e => {\n    e.preventDefault();\n    fb.className = 'form-fb';\n    const name  = document.getElementById('jn-name')?.value.trim() || '';\n    const email = document.getElementById('jn-email')?.value.trim() || '';\n\n    if (!name) { fb.textContent = '\u26a0 Please enter your name.'; fb.classList.add('error'); return; }\n    if (!email || !email.includes('@')) { fb.textContent = '\u26a0 Please enter a valid VIT email.'; fb.classList.add('error'); return; }\n\n    if (btn) { btn.disabled = true; btn.textContent = 'INITIALIZING...'; }\n    setTimeout(() => {\n      if (btn) { btn.disabled = false; btn.innerHTML = 'INITIALIZE MEMBERSHIP &nbsp;\u2192'; }\n      fb.textContent = '\u2713 Application received! Check your email for confirmation.';\n      fb.classList.add('success');\n      form.reset();\n    }, 1500);\n  });\n})();\n\n/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   CINEMATIC HERO YOUTUBE SHUFFLE VIDEO ENGINE\n   Videos provided:\n   1. https://youtu.be/AlySUwPFcNA (Champions 2021)\n   2. https://youtu.be/ZLO9BAA1G0I (Champions 2022)\n   3. https://youtu.be/zX0AV6yxyrQ (Ticking Away)\n   4. https://youtu.be/cMt7RigFfb8 (Masters Tokyo)\n   5. https://youtu.be/kp2UB4Qzsq8 (Champions 2024)\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nconst HERO_VIDEOS = [\n  { id: 'AlySUwPFcNA', title: 'CHAMPIONS // DIE FOR YOU' },\n  { id: 'ZLO9BAA1G0I', title: 'CHAMPIONS // FIRE AGAIN' },\n  { id: 'zX0AV6yxyrQ', title: 'CHAMPIONS // TICKING AWAY' },\n  { id: 'cMt7RigFfb8', title: 'MASTERS // GREATER THAN ONE' },\n  { id: 'kp2UB4Qzsq8', title: 'CHAMPIONS // SUPERPOWER' }\n];\n\n// Shuffle helper\nfunction shuffleList(list) {\n  const arr = [...list];\n  for (let i = arr.length - 1; i > 0; i--) {\n    const j = Math.floor(Math.random() * (i + 1));\n    [arr[i], arr[j]] = [arr[j], arr[i]];\n  }\n  return arr;\n}\n\n// Global player variables\nlet ytPlayer = null;\nlet shuffledVideos = shuffleList(HERO_VIDEOS);\nlet currentClipIndex = 0;\nlet isAudioMuted = true;\nlet clipAutoTimer = null;\n\n// Build YouTube embed URL\nfunction getEmbedUrl(videoId, muted = true) {\n  return `https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&mute=${muted ? 1 : 0}&controls=0&loop=1&playlist=${videoId}&playsinline=1&rel=0&showinfo=0&modestbranding=1&enablejsapi=1&iv_load_policy=3&disablekb=1&fs=0`;\n}\n\n// Update HUD Display\nfunction updateHud(index) {\n  const current = shuffledVideos[index];\n  const titleEl = document.getElementById('hud-clip-title');\n  if (titleEl && current) {\n    titleEl.textContent = `TRAILER ${index + 1}/5 // ${current.title.split('//')[1]?.trim() || current.title}`;\n  }\n\n  // Update dots\n  const dots = document.querySelectorAll('.hud-dot');\n  dots.forEach((dot, i) => {\n    dot.classList.toggle('active', i === index);\n  });\n}\n\n// Switch to a specific clip index\nfunction playClipAtIndex(newIndex) {\n  currentClipIndex = (newIndex + shuffledVideos.length) % shuffledVideos.length;\n  const clip = shuffledVideos[currentClipIndex];\n  if (!clip) return;\n\n  updateHud(currentClipIndex);\n\n  // If YT Player API is ready:\n  if (ytPlayer && typeof ytPlayer.loadVideoById === 'function') {\n    ytPlayer.loadVideoById({\n      videoId: clip.id,\n      startSeconds: 0\n    });\n    if (isAudioMuted) {\n      ytPlayer.mute();\n    } else {\n      ytPlayer.unMute();\n    }\n  } else {\n    // Fallback: update iframe src directly\n    const container = document.getElementById('yt-player');\n    if (container) {\n      container.innerHTML = `<iframe \n        id=\"hero-iframe-inner\"\n        src=\"${getEmbedUrl(clip.id, isAudioMuted)}\" \n        frameborder=\"0\" \n        allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture\" \n        allowfullscreen>\n      </iframe>`;\n    }\n  }\n\n  // Reset auto-advance timer\n  if (clipAutoTimer) clearTimeout(clipAutoTimer);\n  clipAutoTimer = setTimeout(() => {\n    playClipAtIndex(currentClipIndex + 1);\n  }, 45000); // cycle automatically every 45s\n}\n\n// Shuffle to a new random clip\nfunction shuffleToNextClip() {\n  let nextIdx = Math.floor(Math.random() * shuffledVideos.length);\n  if (nextIdx === currentClipIndex && shuffledVideos.length > 1) {\n    nextIdx = (currentClipIndex + 1) % shuffledVideos.length;\n  }\n  playClipAtIndex(nextIdx);\n}\n\n// Toggle Audio\nfunction toggleAudio() {\n  isAudioMuted = !isAudioMuted;\n  const icon = document.getElementById('audio-icon');\n  const label = document.getElementById('audio-label');\n\n  if (icon) icon.textContent = isAudioMuted ? '\ud83d\udd07' : '\ud83d\udd0a';\n  if (label) label.textContent = isAudioMuted ? 'SOUND' : 'MUTED';\n\n  if (ytPlayer && typeof ytPlayer.isMuted === 'function') {\n    if (isAudioMuted) {\n      ytPlayer.mute();\n    } else {\n      ytPlayer.unMute();\n      ytPlayer.setVolume(80);\n    }\n  }\n}\n\n// YouTube Iframe API Callback\nwindow.onYouTubeIframeAPIReady = function () {\n  const target = document.getElementById('yt-player');\n  if (!target) return;\n\n  const firstClip = shuffledVideos[currentClipIndex];\n\n  ytPlayer = new YT.Player('yt-player', {\n    videoId: firstClip.id,\n    playerVars: {\n      autoplay: 1,\n      mute: 1,\n      controls: 0,\n      showinfo: 0,\n      rel: 0,\n      loop: 1,\n      playlist: firstClip.id,\n      playsinline: 1,\n      modestbranding: 1,\n      disablekb: 1,\n      fs: 0,\n      iv_load_policy: 3\n    },\n    events: {\n      onReady: function (event) {\n        event.target.playVideo();\n        event.target.mute();\n        updateHud(currentClipIndex);\n      },\n      onStateChange: function (event) {\n        // When video ends, automatically shuffle to the next\n        if (event.data === YT.PlayerState.ENDED) {\n          playClipAtIndex(currentClipIndex + 1);\n        }\n      }\n    }\n  });\n};\n\n// Initial DOM Setup for Hero Player\ndocument.addEventListener('DOMContentLoaded', () => {\n  const ytContainer = document.getElementById('yt-player');\n  if (ytContainer) {\n    // Inject fallback iframe immediately so there's zero blank flash\n    const firstClip = shuffledVideos[currentClipIndex];\n    ytContainer.innerHTML = `<iframe \n      id=\"hero-iframe-inner\"\n      src=\"${getEmbedUrl(firstClip.id, true)}\" \n      frameborder=\"0\" \n      allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture\" \n      allowfullscreen>\n    </iframe>`;\n    updateHud(currentClipIndex);\n\n    // Auto advance every 45s\n    clipAutoTimer = setTimeout(() => {\n      playClipAtIndex(currentClipIndex + 1);\n    }, 45000);\n  }\n\n  // Shuffle button\n  const shuffleBtn = document.getElementById('btn-shuffle');\n  if (shuffleBtn) {\n    shuffleBtn.addEventListener('click', shuffleToNextClip);\n  }\n\n  // Audio button\n  const audioBtn = document.getElementById('btn-audio');\n  if (audioBtn) {\n    audioBtn.addEventListener('click', toggleAudio);\n  }\n\n  // Direct dot click\n  const dotBtns = document.querySelectorAll('.hud-dot');\n  dotBtns.forEach(dot => {\n    dot.addEventListener('click', () => {\n      const idx = parseInt(dot.dataset.idx, 10);\n      if (!isNaN(idx)) playClipAtIndex(idx);\n    });\n  });\n});\n\n/* \u2500\u2500 LIVE VIEWER COUNT UPDATE \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const base = 6842;\n  const update = () => {\n    const count = base + Math.floor(Math.random() * 300 - 150);\n    const str   = count.toLocaleString();\n    ['viewer-count', 'viewer-count2'].forEach(id => {\n      const el = document.getElementById(id);\n      if (el) el.textContent = str + ' ' + (el.id === 'viewer-count' ? 'LIVE VIEWERS' : 'WATCHING');\n    });\n  };\n  setInterval(update, 5000);\n})();\n\n/* \u2500\u2500 NAVBAR CROSS-PAGE ACTIVE STATE \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const path = window.location.pathname;\n  const filename = path.split('/').pop() || 'index.html';\n  document.querySelectorAll('.nl').forEach(link => {\n    const href = link.getAttribute('href');\n    if (href && href.includes(filename) && filename !== 'index.html') {\n      link.classList.add('active');\n    }\n  });\n})();\n\n/* \u2500\u2500 SECTION STAGGER \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n(function () {\n  const sections = document.querySelectorAll('.section, .lab-section, .dept-section, .coords-section');\n  const obs = new IntersectionObserver(entries => {\n    entries.forEach(entry => {\n      if (!entry.isIntersecting) return;\n      const children = entry.target.querySelectorAll('.ri:not(.visible)');\n      children.forEach((child, i) => {\n        setTimeout(() => child.classList.add('visible'), i * 60);\n      });\n    });\n  }, { threshold: 0.04 });\n  sections.forEach(s => obs.observe(s));\n})();\n", "Description": "Shared JavaScript with YouTube cinematic background video player, shuffle system, and audio toggle", "Overwrite": true, "TargetFile": "e:\\vrgc-live\\script.js", "toolAction": "Updating script.js with video engine", "toolSummary": "Adding video shuffle engine to script.js"}}]}